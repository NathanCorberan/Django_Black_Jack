export default function usePlayGame() {
    const rollDice = (nbr_dice, playerscore) => {
        scrore = playerscore;
        for (let i = 0; i < nbr_dice; i++) {
            scrore += Math.floor((Math.random() * 6) + 1)
            return randomNumber;
        }
    };
    
}